# Backend API URL — leave empty for local dev (Vite proxy handles it)
# Set this in production to your deployed backend URL, e.g.:
# VITE_API_URL="https://your-backend.railway.app/api"
VITE_API_URL=
